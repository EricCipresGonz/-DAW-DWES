Bueno podemos observar que he hecho un form para crear un formulario el cual pide que escribas un texto
y entonces al enviar-lo , y en el php se envia el formulario por el POST y se recoge el contenido del
texto cadena y lo enviamos por el POST y entonces se muestra el texto introducido y despues se muestra el 
numero de letras que hay.