Bueno en este codigo comprobamos si existe una cookie llamada contador. Y si existe se otiene su valor ys e convierte en entero
a traves del int. Y si no existe se restablece a 0 el contador
Cada vez que la pagina se carga el contador incrementa un numero.Y tambien añado un setcookie que tiene un tiempo de unos
3600 segundos . Y por ultimo un echo que imprima el numero de vezes actualizado de el contador.